% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity.R
\name{tcr_repertoire_overlap}
\alias{tcr_repertoire_overlap}
\title{Repertoire overlap metrics}
\usage{
tcr_repertoire_overlap(
  counts_a,
  counts_b,
  metrics = c("jaccard", "morisita_horn", "overlap_coef")
)
}
\arguments{
\item{counts_a, counts_b}{Named numeric vectors of clonotype counts.
Names are clonotype identifiers.}

\item{metrics}{Character vector. Which metrics to compute. Default
computes all three.}
}
\value{
A named list with the requested metrics, each a scalar in
\code{[0, 1]}.
}
\description{
Computes overlap metrics between two named count vectors representing
clonotype abundances from two samples. Available metrics:
}
\details{
\describe{
\item{Jaccard index}{\code{|A intersect B| / |A union B|} --- proportion
of shared clonotype species (presence/absence).}
\item{Morisita-Horn}{\code{2 * sum(p_a * p_b) / (sum(p_a^2) + sum(p_b^2))}
--- abundance-weighted overlap.}
\item{Overlap coefficient}{\code{|A intersect B| / min(|A|, |B|)} ---
overlap normalized by the smaller set.}
}
}
\examples{
a <- c(clone1 = 10, clone2 = 5, clone3 = 1)
b <- c(clone1 = 8, clone3 = 3, clone4 = 2)
tcr_repertoire_overlap(a, b)

}
\seealso{
\code{\link{tcr_diversity}}, \code{\link{tcr_clonality}}
}
