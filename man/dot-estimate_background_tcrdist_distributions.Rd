% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tcr_clumping.R
\name{.estimate_background_tcrdist_distributions}
\alias{.estimate_background_tcrdist_distributions}
\title{Estimate background paired TCRdist distributions}
\usage{
.estimate_background_tcrdist_distributions(
  organism,
  tcr_df,
  max_dist,
  num_random_samples = 50000L,
  pseudocount = 0.25,
  preserve_vj_pairings = FALSE,
  bg_tcrs = NULL
)
}
\arguments{
\item{organism}{Character string. Organism key (e.g. \code{"human"}).}

\item{tcr_df}{A data.frame with columns \code{va, ja, cdr3a, cdr3a_nucseq,
vb, jb, cdr3b, cdr3b_nucseq}.}

\item{max_dist}{Integer. Maximum paired TCRdist to consider.}

\item{num_random_samples}{Integer. Number of random background chains to
generate per chain type.}

\item{pseudocount}{Numeric. Pseudocount added before normalizing.}

\item{preserve_vj_pairings}{Logical. Preserve V-J pairings in resampling.}

\item{bg_tcrs}{Optional data.frame used for background generation.}
}
\value{
Numeric matrix of dimensions \code{nrow(tcr_df) x (max_dist + 1)}.
}
\description{
For each foreground TCR, estimates the probability of seeing a paired
TCRdist score at or below each integer distance from 0 to \code{max_dist}
under a null model where alpha and beta chains are independently drawn
from shuffled backgrounds.
}
\keyword{internal}
