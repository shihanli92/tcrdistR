% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_neighbors.R
\name{knn_from_matrix}
\alias{knn_from_matrix}
\title{K-nearest-neighbors from a precomputed distance matrix}
\usage{
knn_from_matrix(D, K, agroups = NULL, bgroups = NULL, sort_nbrs = TRUE)
}
\arguments{
\item{D}{Numeric matrix (N x N). Precomputed pairwise distance matrix.
Must be square.}

\item{K}{Integer. Number of nearest neighbors to return.
Must satisfy \code{1 <= K <= N - 1}.}

\item{agroups}{Integer vector of length N, or \code{NULL}. Alpha-chain
group assignments. \code{NULL} assigns unique groups (no masking).}

\item{bgroups}{Integer vector of length N, or \code{NULL}. Beta-chain
group assignments.}

\item{sort_nbrs}{Logical. If \code{TRUE} (default), sort K neighbors by
ascending distance.}
}
\value{
A \code{list} with two elements:
\describe{
\item{\code{knn_indices}}{Integer matrix (N x K). 1-based indices.}
\item{\code{knn_distances}}{Numeric matrix (N x K). Distances.}
}
}
\description{
Extracts K nearest neighbors for each row of a precomputed N x N distance
matrix \code{D}, with optional same-group masking.
}
\examples{
\donttest{
tcrs <- data.frame(
  va    = c("TRAV1-1*01", "TRAV1-1*01"),
  cdr3a = c("CAVRDSSYKLIF", "CAVRDSSYKLIF"),
  vb    = c("TRBV19*01", "TRBV19*01"),
  cdr3b = c("CASSIRSSYEQYF", "CASSIRSYEQYF"),
  stringsAsFactors = FALSE
)
D   <- tcrdist_matrix(tcrs, "human")
knn <- knn_from_matrix(D, K = 1L)
}
}
