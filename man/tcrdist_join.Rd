% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tcr_join.R
\name{tcrdist_join}
\alias{tcrdist_join}
\title{Join two TCR datasets by TCRdist threshold}
\usage{
tcrdist_join(
  left_df,
  right_df,
  organism,
  radius,
  max_n = 5L,
  type = c("inner", "left"),
  suffix = c("_x", "_y"),
  rect_dist_matrix = NULL
)
}
\arguments{
\item{left_df}{Data.frame with TCR columns (\code{va}, \code{vb},
\code{cdr3a}, \code{cdr3b}) plus any extra columns.}

\item{right_df}{Data.frame with TCR columns (\code{va}, \code{vb},
\code{cdr3a}, \code{cdr3b}) plus any extra columns.}

\item{organism}{Character string (\code{"human"} or \code{"mouse"}).}

\item{radius}{Numeric. Maximum TCRdist for a match.}

\item{max_n}{Integer. Maximum number of matches per left row. Default
\code{5L}. Closest matches are kept.}

\item{type}{Character string. Join type: \code{"inner"} (default) keeps
only matched pairs, \code{"left"} keeps all left rows (NAs for
unmatched).}

\item{suffix}{Character vector of length 2. Suffixes for disambiguating
column names. Default \code{c("_x", "_y")}.}

\item{rect_dist_matrix}{Optional precomputed rectangular distance matrix
(rows = left, cols = right). If provided, \code{organism} is not used
for distance computation.}
}
\value{
A data.frame with columns from both sides (suffixed if
overlapping) plus a \code{tcrdist} column.
}
\description{
Performs a fuzzy merge of two TCR data.frames: for each row in
\code{left_df}, finds rows in \code{right_df} within \code{radius}
TCRdist units. Similar to a SQL JOIN but using distance instead of
exact matching.
}
\examples{
\dontrun{
matches <- tcrdist_join(query_tcrs, reference_tcrs, "human", radius = 50)
}

}
\seealso{
\code{\link{tcrdist_rect}}, \code{\link{tcrdist_radius_neighbors}}
}
