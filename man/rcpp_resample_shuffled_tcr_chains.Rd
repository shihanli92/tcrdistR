% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rcpp_resample_shuffled_tcr_chains}
\alias{rcpp_resample_shuffled_tcr_chains}
\title{Resample shuffled TCR chains by junction breakpoint splicing (C++ implementation)}
\usage{
rcpp_resample_shuffled_tcr_chains(
  junction_v_genes,
  junction_j_genes,
  junction_nucseqs,
  junction_breakpoints_pre_d,
  junction_breakpoints_post_d,
  chain,
  num_samples,
  max_attempts
)
}
\arguments{
\item{junction_v_genes}{Character vector. V gene names for each junction.}

\item{junction_j_genes}{Character vector. J gene names for each junction.}

\item{junction_nucseqs}{Character vector. CDR3 nucleotide sequences.}

\item{junction_breakpoints_pre_d}{List of integer vectors. Pre-D breakpoints
for each junction.}

\item{junction_breakpoints_post_d}{List of integer vectors. Post-D
breakpoints for each junction.}

\item{chain}{Character string. \code{"A"} for alpha (pre-D breakpoints only)
or \code{"B"} for beta (both pre-D and post-D).}

\item{num_samples}{Integer. Number of chimeric TCRs to generate.}

\item{max_attempts}{Integer. Maximum sampling attempts before returning
partial results.}
}
\value{
A data.frame with columns: \code{v_gene}, \code{j_gene},
\code{cdr3} (amino acid), \code{cdr3_nucseq}.
}
\description{
Randomly selects pairs of junctions, finds shared breakpoints, and splices
chimeric nucleotide sequences.  Validates that the result has a length
divisible by 3 and contains no stop codons, then translates to amino acid.
}
\details{
Uses an inline standard genetic code table (no external codon map needed).
}
\examples{
\dontrun{
  result <- rcpp_resample_shuffled_tcr_chains(
    v_genes, j_genes, nucseqs, bp_pre, bp_post, "B", 1000L, 100000L
  )
}
}
