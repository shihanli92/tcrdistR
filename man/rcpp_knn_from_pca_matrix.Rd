% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rcpp_knn_from_pca_matrix}
\alias{rcpp_knn_from_pca_matrix}
\title{K-nearest-neighbors from a PCA embedding matrix (C++ implementation)}
\usage{
rcpp_knn_from_pca_matrix(pca_matrix, K, agroups, bgroups, sort_nbrs = TRUE)
}
\arguments{
\item{pca_matrix}{Numeric matrix (N x D). PCA or other embedding coordinates.
Rows are samples, columns are dimensions.}

\item{K}{Integer. Number of nearest neighbors to extract per point.}

\item{agroups}{Integer vector of length N. Alpha-chain group assignments.}

\item{bgroups}{Integer vector of length N. Beta-chain group assignments.}

\item{sort_nbrs}{Logical. If \code{TRUE}, sort the K neighbors by ascending
Euclidean distance.  Default \code{TRUE}.}
}
\value{
A \code{List} with two elements:
\describe{
\item{\code{knn_indices}}{Integer matrix (N x K). 1-based neighbor indices.}
\item{\code{knn_distances}}{Numeric matrix (N x K). Euclidean distances.}
}
}
\description{
Computes K nearest neighbors directly from PCA embeddings (N x D matrix)
without materializing the full N x N Euclidean distance matrix.  For each
row, computes Euclidean distances to all N points on the fly, applies
group masking, and extracts the K nearest via \code{std::nth_element}.
}
\details{
Memory: O(N\emph{D + N}K) instead of O(N^2).
}
\examples{
\dontrun{
  pca <- matrix(rnorm(30), nrow=10, ncol=3)
  result <- rcpp_knn_from_pca_matrix(pca, K=3L, agroups=1:10, bgroups=1:10)
}
}
\keyword{internal}
