% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_logo.R
\name{plot_gene_alluvial}
\alias{plot_gene_alluvial}
\title{Alluvial plot of TCR gene segment usage}
\usage{
plot_gene_alluvial(
  tcr_df = NULL,
  axes = c("ja", "va", "vb", "jb"),
  mode = c("sankey", "alluvial"),
  strip_allele = TRUE,
  max_genes = 15L,
  title = NULL,
  color_by = NULL,
  fill_color = "black",
  alpha = 0.4,
  palette = NULL,
  facet_by = NULL,
  tcr_rep = NULL
)
}
\arguments{
\item{tcr_df}{Data.frame with gene columns (e.g. \code{va}, \code{ja},
\code{vb}, \code{jb}).}

\item{axes}{Character vector of column names to use as axes (strata),
in display order.  Default \code{c("ja", "va", "vb", "jb")}.}

\item{strip_allele}{Logical.  If \code{TRUE} (default), strip allele
suffixes before tallying.}

\item{max_genes}{Integer.  Per-axis cap: only the top \code{max_genes}
genes are shown; the rest are collapsed to \code{"Other"}.
Default \code{15L}.}

\item{title}{Optional plot title.}

\item{color_by}{Column name (character string) to color the alluvial
flows by.  Must be one of the \code{axes}.  Set to \code{NULL} for
a uniform fill color (see \code{fill_color}).  Default \code{NULL}
(uniform fill).}

\item{fill_color}{Single color string used for all flows when
\code{color_by} is \code{NULL}.  Default \code{"black"}.  Ignored
when \code{color_by} is set.}

\item{alpha}{Numeric.  Flow opacity.  Default \code{0.4}.}

\item{palette}{Character vector of colors, or \code{NULL} for the
default \code{.tcrdistR_palette}.  Only used when \code{color_by}
is not \code{NULL}.}

\item{facet_by}{Optional column name (character string) or vector of
values to facet by.  When a single string that matches a column in
\code{tcr_df}, that column is used for \code{facet_wrap}.  Otherwise
treated as a vector of facet labels (must have length
\code{nrow(tcr_df)}).}

\item{tcr_rep}{A \code{\linkS4class{TCRrep}} object.  If provided, the
clone data.frame is used as \code{tcr_df}.  Cannot be combined with
\code{tcr_df}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws an alluvial (Sankey) diagram showing how clonotypes flow across
gene segments.  The default axis order is
\code{ja} \eqn{\rightarrow} \code{va} \eqn{\rightarrow} \code{vb}
\eqn{\rightarrow} \code{jb}, but any subset or reordering can be
specified via \code{axes}.
}
\details{
Requires the \pkg{ggalluvial} package (Suggests).
}
\examples{
\dontrun{
data(dash)
plot_gene_alluvial(dash)
plot_gene_alluvial(dash, color_by = "va")
plot_gene_alluvial(dash, fill_color = "steelblue", alpha = 0.5)
plot_gene_alluvial(dash, facet_by = "epitope")
}

}
\seealso{
\code{\link{plot_gene_usage}}, \code{\link{plot_cdr3_length}}
}
