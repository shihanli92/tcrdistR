% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rcpp_tcrdist_matrix}
\alias{rcpp_tcrdist_matrix}
\title{Pairwise TCRdist matrix (C++ implementation)}
\usage{
rcpp_tcrdist_matrix(
  va_genes,
  cdr3a_seqs,
  vb_genes,
  cdr3b_seqs,
  v_dist_a,
  v_dist_b,
  weight_cdr3_region = 3L,
  gap_penalty_cdr3_region = 12L
)
}
\arguments{
\item{va_genes}{Character vector of length N. Alpha V-gene allele names.}

\item{cdr3a_seqs}{Character vector of length N. Alpha CDR3 sequences.}

\item{vb_genes}{Character vector of length N. Beta V-gene allele names.}

\item{cdr3b_seqs}{Character vector of length N. Beta CDR3 sequences.}

\item{v_dist_a}{Named square \code{NumericMatrix}. Pre-computed pairwise
V-region distances for alpha genes (row/column names are V-gene allele
strings, e.g. \code{"TRAV1-1*01"}).}

\item{v_dist_b}{Named square \code{NumericMatrix}. Same structure for
beta genes.}

\item{weight_cdr3_region}{Integer. CDR3 alignment distance multiplier.
Default 3 matches \code{WEIGHT_CDR3_REGION}.}

\item{gap_penalty_cdr3_region}{Integer. Per-residue length-difference
penalty.  Default 12 matches \code{GAP_PENALTY_CDR3_REGION}.}
}
\value{
An N x N symmetric \code{NumericMatrix} of TCRdist distances.
}
\description{
Computes the full N x N symmetric matrix of paired-chain TCRdist distances.
For each pair \code{(i, j)}:
\deqn{
  d(i,j) = v\_dist\_a[va_i, va_j] + cdr3\_dist(cdr3a_i, cdr3a_j)
          + v\_dist\_b[vb_i, vb_j] + cdr3\_dist(cdr3b_i, cdr3b_j)
}
}
\details{
Only the upper triangle is computed; it is mirrored to the lower triangle.
The diagonal is zero.  \code{Rcpp::checkUserInterrupt()} is called every
100 outer-loop rows to allow the user to interrupt long computations.

BSD4 values are compiled in as \code{constexpr}; no \code{bsd4} matrix
argument is needed (unlike the rconga equivalent).
}
\examples{
\dontrun{
  mat <- rcpp_tcrdist_matrix(
    va_genes = c("TRAV1-1*01", "TRAV1-2*01"),
    cdr3a_seqs = c("CAVSANSGTYF", "CAVSANSGTYF"),
    vb_genes = c("TRBV20-1*01", "TRBV20-1*01"),
    cdr3b_seqs = c("CASSIRSSYEQYF", "CASSIRSYEQYF"),
    v_dist_a = v_alpha_mat,
    v_dist_b = v_beta_mat
  )
}
}
\keyword{internal}
