% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_neighbors.R
\name{knn_from_pca}
\alias{knn_from_pca}
\title{K-nearest-neighbors from a PCA embedding matrix}
\usage{
knn_from_pca(pca_matrix, K, agroups = NULL, bgroups = NULL, sort_nbrs = TRUE)
}
\arguments{
\item{pca_matrix}{Numeric matrix (N x D). Rows are samples, columns are
embedding dimensions.}

\item{K}{Integer. Number of nearest neighbors. Must satisfy
\code{1 <= K <= N - 1}.}

\item{agroups}{Integer vector of length N, or \code{NULL}. \code{NULL}
assigns unique groups (no masking).}

\item{bgroups}{Integer vector of length N, or \code{NULL}.}

\item{sort_nbrs}{Logical. If \code{TRUE} (default), sort K neighbors by
ascending Euclidean distance.}
}
\value{
A \code{list} with two elements:
\describe{
\item{\code{knn_indices}}{Integer matrix (N x K). 1-based indices.}
\item{\code{knn_distances}}{Numeric matrix (N x K). Euclidean distances.}
}
}
\description{
Computes K nearest neighbors by Euclidean distance from an N x D PCA
(or other embedding) matrix, without constructing the full N x N distance
matrix.  Optional same-group masking applies.
}
\examples{
\donttest{
pca <- matrix(rnorm(30), nrow = 10, ncol = 3)
knn <- knn_from_pca(pca, K = 3L)
}
}
