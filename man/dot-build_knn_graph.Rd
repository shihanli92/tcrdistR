% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umap.R
\name{.build_knn_graph}
\alias{.build_knn_graph}
\title{Build a fuzzy simplicial set graph from KNN data}
\usage{
.build_knn_graph(knn_indices, knn_distances, n_cells)
}
\arguments{
\item{knn_indices}{Integer matrix (N x K).  1-based neighbor indices (as
returned by \code{\link{tcrdist_knn}}).}

\item{knn_distances}{Numeric matrix (N x K).  Corresponding distances.}

\item{n_cells}{Integer scalar.  Total number of observations.}
}
\value{
Sparse \code{dgCMatrix} (N x N) with symmetrized fuzzy membership
weights.
}
\description{
Constructs a sparse N x N adjacency matrix using UMAP's fuzzy simplicial
set algorithm.  Edge weights are \code{exp(-(d - rho) / sigma)} and the
graph is symmetrized via fuzzy union: \code{W + W^T - W * W^T}.  Port of
rconga's \code{.build_knn_graph()}, adapted for 1-based indices.
}
\keyword{internal}
