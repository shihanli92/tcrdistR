% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical.R
\name{tcrdist_hclust}
\alias{tcrdist_hclust}
\title{Hierarchical clustering of TCRs by TCRdist}
\usage{
tcrdist_hclust(tcr_df, organism, method = "average", max_tcrs = 2000L)
}
\arguments{
\item{tcr_df}{Data.frame with TCR columns.}

\item{organism}{Character string (\code{"human"} or \code{"mouse"}).}

\item{method}{Clustering method for \code{stats::hclust()}. Default
\code{"average"} (UPGMA).}

\item{max_tcrs}{Integer. Subsample if N exceeds this. Default \code{2000L}.}
}
\value{
A named list:
\describe{
\item{\code{hclust}}{An \code{hclust} object.}
\item{\code{dist_matrix}}{The pairwise distance matrix used.}
\item{\code{indices}}{Integer vector of row indices used (after
potential subsampling).}
}
}
\description{
Convenience wrapper that computes the pairwise TCRdist matrix and
performs hierarchical clustering via \code{stats::hclust()}.
}
\examples{
\dontrun{
result <- tcrdist_hclust(tcr_df, "human")
plot(result$hclust)
clusters <- cutree(result$hclust, k = 5)
}

}
\seealso{
\code{\link{cluster_tcrs}}, \code{\link{neighborhood_test}}, \code{\link{plot_tcrdist_dendrogram}}
}
