% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_logo.R
\name{plot_cdr3_logo}
\alias{plot_cdr3_logo}
\title{Plot a CDR3 sequence logo}
\usage{
plot_cdr3_logo(
  cdr3_seqs,
  chain = c("alpha", "beta"),
  trim = TRUE,
  method = c("prob", "bits"),
  gap_character = "-",
  title = NULL,
  nucseq_src = NULL,
  show_junction_bars = FALSE
)
}
\arguments{
\item{cdr3_seqs}{Character vector. CDR3 amino acid sequences.}

\item{chain}{Character string. \code{"alpha"} or \code{"beta"}.}

\item{trim}{Logical. If \code{TRUE}, trim conserved C/F residues before
alignment (first 3, last 2 characters). Default \code{TRUE}.}

\item{method}{Character string. \code{"prob"} for frequency-based logo,
\code{"bits"} for information content. Default \code{"prob"}.}

\item{gap_character}{Character string. Gap character for alignment.
Default \code{"-"}.}

\item{title}{Optional plot title. If \code{NULL}, defaults to the chain
name with Greek letter.}

\item{nucseq_src}{List of character vectors with nucleotide source labels
(one per CDR3). Required for junction bars.}

\item{show_junction_bars}{Logical. If \code{TRUE} and \code{nucseq_src}
is provided, stack junction bars below the logo via \pkg{patchwork}.}
}
\value{
A \code{ggplot} object (or \code{patchwork} object if junction bars
are included).
}
\description{
Renders a sequence logo from CDR3 amino acid sequences using
\pkg{ggseqlogo}. Two methods are available: \code{"bits"} shows information
content per position, \code{"prob"} shows amino acid frequencies.
}
\examples{
\dontrun{
seqs <- c("CAVRDSSYKLIF", "CAVKDSSYKLIF", "CAVRDSYKLIF",
          "CAVRDSSYKLIF", "CAVKDSYKLIF")
plot_cdr3_logo(seqs, chain = "alpha", method = "bits")
plot_cdr3_logo(seqs, chain = "beta", method = "prob")
}

}
