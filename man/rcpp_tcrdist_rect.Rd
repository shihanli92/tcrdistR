% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rcpp_tcrdist_rect}
\alias{rcpp_tcrdist_rect}
\title{Rectangular TCRdist query-vs-reference distance matrix (C++ implementation)}
\usage{
rcpp_tcrdist_rect(
  query_va,
  query_cdr3a,
  query_vb,
  query_cdr3b,
  ref_va,
  ref_cdr3a,
  ref_vb,
  ref_cdr3b,
  v_dist_a,
  v_dist_b,
  weight_cdr3_a = 3L,
  gap_penalty_cdr3_a = 12L,
  weight_cdr3_b = 3L,
  gap_penalty_cdr3_b = 12L
)
}
\arguments{
\item{query_va}{Character vector of length nq. Query alpha V-gene alleles.}

\item{query_cdr3a}{Character vector of length nq. Query alpha CDR3 sequences.}

\item{query_vb}{Character vector of length nq. Query beta V-gene alleles.}

\item{query_cdr3b}{Character vector of length nq. Query beta CDR3 sequences.}

\item{ref_va}{Character vector of length nr. Reference alpha V-gene alleles.}

\item{ref_cdr3a}{Character vector of length nr. Reference alpha CDR3 sequences.}

\item{ref_vb}{Character vector of length nr. Reference beta V-gene alleles.}

\item{ref_cdr3b}{Character vector of length nr. Reference beta CDR3 sequences.}

\item{v_dist_a}{Named square \code{NumericMatrix}. Pre-computed pairwise
V-region distances for alpha genes.}

\item{v_dist_b}{Named square \code{NumericMatrix}. Pre-computed pairwise
V-region distances for beta genes.}

\item{weight_cdr3_region}{Integer. CDR3 alignment distance multiplier.
Default 3 matches \code{WEIGHT_CDR3_REGION}.}

\item{gap_penalty_cdr3_region}{Integer. Per-residue length-difference
penalty. Default 12 matches \code{GAP_PENALTY_CDR3_REGION}.}
}
\value{
An nq x nr \code{NumericMatrix} of TCRdist distances.
}
\description{
Computes an nq x nr matrix of paired-chain TCRdist distances between a
query set of \code{nq} TCRs and a reference set of \code{nr} TCRs.
For each query-reference pair \code{(i, j)}:
\deqn{
  d(i,j) = v\_dist\_a[va\_query_i, va\_ref_j]
          + cdr3\_dist(cdr3a\_query_i, cdr3a\_ref_j)
          + v\_dist\_b[vb\_query_i, vb\_ref_j]
          + cdr3\_dist(cdr3b\_query_i, cdr3b\_ref_j)
}
}
\details{
No symmetry is exploited; all nq * nr distances are computed.
\code{Rcpp::checkUserInterrupt()} is called every 100 outer-loop rows.
}
\examples{
\dontrun{
  mat <- rcpp_tcrdist_rect(
    query_va = c("TRAV1-1*01"),
    query_cdr3a = c("CAVSANSGTYF"),
    query_vb = c("TRBV20-1*01"),
    query_cdr3b = c("CASSIRSSYEQYF"),
    ref_va = c("TRAV1-1*01", "TRAV1-2*01"),
    ref_cdr3a = c("CAVSANSGTYF", "CAVSANSGTYF"),
    ref_vb = c("TRBV20-1*01", "TRBV20-1*01"),
    ref_cdr3b = c("CASSIRSSYEQYF", "CASSIRSYEQYF"),
    v_dist_a = v_alpha_mat,
    v_dist_b = v_beta_mat
  )
}
}
\keyword{internal}
