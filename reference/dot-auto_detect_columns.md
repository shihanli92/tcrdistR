# Auto-detect and rename common TCR column naming patterns

Auto-detect and rename common TCR column naming patterns

## Usage

``` r
.auto_detect_columns(df)
```

## Arguments

- df:

  A `data.frame` as read from a file.

## Value

The `data.frame` with columns renamed to canonical names.
