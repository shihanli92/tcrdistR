# Resolve a components specification into per-component flags

Resolve a components specification into per-component flags

## Usage

``` r
.resolve_components(components)
```

## Arguments

- components:

  Character. A single preset string or a vector of individual component
  names.

## Value

A named logical list with elements `va`, `cdr3a`, `vb`, `cdr3b`.
