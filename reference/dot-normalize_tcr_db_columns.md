# Normalize TCR database column names

Renames `va_gene` to `va`, `vb_gene` to `vb`, etc.

## Usage

``` r
.normalize_tcr_db_columns(df)
```

## Arguments

- df:

  A data.frame.

## Value

The same data.frame with normalized column names.
